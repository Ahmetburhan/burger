<div class="row">
  <div class="col s12">
    <ul class=" card light-green">
      {{#each itm}}
            <p>
              {{this.id}}. {{this.item}}
            </p>
      {{/each}}
    </ul>
  </div>