<div class="row">
  <div class="col s3"></div>
  <div class="col s6">
    <ul class="card center-align light-green">
      <h5>Burgers</h5>
        <div class="col s2"></div>
        <div class="col s8 left-align">
        {{#each itm}}
          {{#if this.entree}}
              <p>
                {{this.id}}. {{this.item}}
              </p>
          {{/if}}
        {{/each}}
        </div>
    </ul>
  </div>
</div>

<div class="row">
  <div class="col s3"></div>
  <div class="col s6">
    <ul class="card center-align light-green">
      <h5>Sides</h5>
        <div class="col s2"></div>
        <div class="col s8 left-align">
        {{#each itm}}
          {{#unless entree}}
              <p>
                {{this.id}}. {{this.item}}
              </p>
          {{/unless}}
        {{/each}}
        </div>
    </ul>
  </div>
